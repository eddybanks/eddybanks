/** nav **/

nav {
  @include font_props($aref_ruqaa, 30px, $honeydew);
  z-index: 1000;
}

header {
  @include font_props($indie_flower, 40px);
  .page-header {
    border-bottom: none;
  }
}

.jumbotron {
  background-color: rgba($black, 0.5);
  margin-bottom: 0px;
  // padding-bottom: 0px;
}
