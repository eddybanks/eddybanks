<section class="skills index">
  <!-- <div class="ui divided items">
    <%# @skills.each do |skill| %>
      <div class="item">
        <h5 class="header"><%#= skill.name %></h4>
      </div>
    <%# end %>
  </div> -->
  <div class="ui segment">
    <% @skills.each do |skill| %>
      <div class="ui accordion">
        <div class="title">
          <i class="dropdown icon"></i>
          <%= skill.name %>
        </div>
        <div class="content">
          <%= skill.description %>
        </div>
      </div>
    <% end %>
  </div>
</section>
